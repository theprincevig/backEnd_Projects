<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit user</title>
</head>
<style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:ital,wght@500;600;700;900&display=swap');

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        transition: 0.3s;
    }
    body {
        width: 100%;
        height: 100dvh;
        background: linear-gradient(120deg, #191922, #101016);
        color: #fff;
        font-family: 'Poppins', sans-serif;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }

    h2 {
        margin-bottom: 5%;
    }
    form {
        width: 25%;
        height: 40%;
        border: solid 2px #FFFFFF1A;
        background: linear-gradient(to top, #9999990D, #FFFFFF0D);
        display: flex;
        flex-direction: column;
        padding: 5px 10px;
        border-radius: 12px;
    }
    textarea {
        width: 100%;
        height: 20%;
        padding: 10px;
        font-size: 18px;
        border: none;
        outline: none;
        margin-bottom: 10%;
    }
    input {
        width: 100%;
        padding: 18px 15px;
        border-radius: 20px;
        border: none;
        outline: none;
        font-size: 16px;
    }
    textarea:hover, input:hover {
        transform: translateY(5px);
    }
    h4 {
        margin: 10% 0 5px 5px;
        color: #FFFFFFB3;
        font-size: 16px;
        cursor: default;
        transition: 0.75s;
    }
    .password-message {
        margin: 0 10px 5px;
        color: #FFFFFFB3;
        font-size: 12px;
        transition: 0.75s;
        cursor: default;
    }
    h4:hover, .password-message:hover {
        color: #fff;
    }
    button {
        width: 45%;
        margin: 5% auto 0;
        padding: 10px 15px;
        border-radius: 10px;
        border: none;
        outline: none;
        background: #490CCC;
        color: #fff;
        cursor: pointer;
        font-size: 20px;
        font-weight: 600;
    }
    button:hover { background: #32098F; }
    button:active {
        opacity: 0.5;
        transform: scale(0.8);
    }
</style>
<body>
    <h2>You're about to edit this user : <%= user.email %></h2>
    <form method="POST" action="/user/<%= user.id %>?_method=PATCH">
        <h4>Change your username :</h4>
        <textarea placeholder="change your username" name="username"><%= user.username %></textarea>
        <p class="password-message">Please enter your current password to confirm that your username will be changed.</p>
        <input name="password" placeholder="enter your password" type="password">
        <button>confirm</button>
    </form>
</body>
</html>